{{!< default}}

<main class="gh-main gh-outer">
    <div class="gh-inner">


        <section class="gh-pagehead">
        </section>

        <div class="gh-topic gh-topic-grid gh-topic-grid-item">
            <div class="gh-topic-content gh-feed">
                {{#foreach posts}}
                    {{> "loop-grid" has_large_post=false}}
                {{/foreach}}
            </div>

            {{!-- Without this, a reader with no JS reaches /page/2 and has no
                way to page 3 - infinite scroll was the only path onward. Same
                hidden-when-scripting treatment as home.hbs. --}}
            {{#if pagination.next}}
            <footer class="gh-topic-footer">
                <a class="gh-topic-link gh-loadmore" href="{{@site.url}}/page/{{pagination.next}}/">{{t "Show more"}} {{> "icons/arrow"}}</a>
            </footer>
            {{/if}}
        </div>

    </div>
</main>