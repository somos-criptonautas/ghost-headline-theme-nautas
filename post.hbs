{{!< default}} <main class="gh-main">
    <div class="gh-inner">
        {{#post}}
        <article class="gh-article {{post_class}}">
            <header class="gh-article-header gh-canvas">
                {{> "breadcrumb"}}

                <h1 class="gh-article-title">{{title}}</h1>

                {{#if custom_excerpt}}
                <p class="gh-article-excerpt">{{custom_excerpt}}</p>
                {{/if}}

                {{#if feature_image}}
                <figure class="gh-article-image">
                    <img class="gh-feature-image" loading="lazy" srcset="{{img_url feature_image size="s" format="webp" }} 300w, {{img_url
                        feature_image size="m" format="webp" }} 720w, {{img_url feature_image size="l" format="webp" }}
                        960w, {{img_url feature_image size="xl" format="webp" }} 1200w, {{img_url feature_image
                        size="xxl" format="webp" }} 2000w" sizes="(max-width: 1200px) 100vw, 1200px"
                        src="{{img_url feature_image size="xl" format="webp" }}" alt="{{title}}">
                    {{#if feature_image_caption}}
                    <figcaption>{{feature_image_caption}}</figcaption>
                    {{/if}}
                </figure>
                {{/if}}
            </header>
            {{#is "post"}}
            <div class="gh-author">

                {{> "post-meta"}}
            </div>
            <div class="gh-full-content">

                <aside class="gh-sidebar" id="sidebar-toc">
                    {{#if reading_time}}
                    <h3>
                        {{reading_time minute="1 min de lectura" minutes="% mins de lectura"}}
                    </h3>
                    {{/if}}
                    <div class="toc">
                    </div>
                </aside>

                <section class="gh-content gh-canvas">
                    {{content}}
                    {{> "comments"}}
                </section>
            </div>
            {{>webmentions}}
            {{/is}}
    </article>
    {{/post}}
    {{> "related-posts"}}
    </div>
    </main>